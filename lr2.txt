#include <iostream>
#include <cmath>
using namespace std;
int fact(int v)
{
    if(v<0) return 0;
    if(v==0) return 1;
    else return v*fact(v-1);
}




int main() {
    char q = 0;
    for (int i = 0; i <= 255; ++i) {
        cout << q << endl;
        q++;
    }
    cout << "Enter symbol" << endl;
    char w = 0;
cin>>w;
    if ((w >= 48) && (w <= 57))
        cout << "digit" << endl;

    if ((w >= 65) && (w <= 90))
        cout << "word";
    else
        cout << "dont understand this symbol" << endl;



    int x;
    cout << "Enter x value" << endl;
    cin >> x;
    double y;
    y = 0;
    int z = x % 5;
    switch (z) {
        case 0:
            y = -1;
            break;
        case 1:
            y = x;
            break;
        case 2:
            y = 1 / x;
            break;
        case 3:
            y = x;
            break;
        case 4:
            y = x * x;
            break;
    }
    cout << y << endl;
    cout << "enter x value" << endl;
    double x1;
    cin >> x1;
    int n;
    y = cos(x1);
    long double h;
    double u = 1.0;
    for (int i = -2; i >= -6; --i) {
        n = 0;
        h = 1.0 * x1;
        while (abs(h - y) >= 0.001 * u) {
            n++;
            h += x - pow(x, 2) / !2 + pow(x, 4) / !4 - pow(-1, n) * pow(x, 2 * n) / !(2 * n);
            cout << y << " " << endl;
        }

        cout << "for 10^" << i << "need iteration" << n << endl;
        cout << "for 10^ need iteration" << n << endl;
    }
    return 0;
}



